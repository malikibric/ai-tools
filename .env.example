GOOGLE_API_KEY=
# Optional: override the Gemini model used for structured analysis (default: gemini-3.5-flash-lite)
# GOOGLE_MODEL=gemini-3.5-flash-lite

# Postgres connection string (Supabase, Railway, etc). Required — all four tools persist through Prisma.
DATABASE_URL=
